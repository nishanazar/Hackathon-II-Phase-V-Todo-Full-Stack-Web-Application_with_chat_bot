Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get all {{ .Release.Name }}

To access the frontend service:
  export FRONTEND_PORT=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "todo-app.fullname" . }}-frontend -o jsonpath="{.spec.ports[0].port}")
  export FRONTEND_HOST=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "todo-app.fullname" . }}-frontend -o jsonpath="{.spec.clusterIP}")
  echo "Visit http://$FRONTEND_HOST:$FRONTEND_PORT to access the frontend"

To access the backend service:
  export BACKEND_PORT=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "todo-app.fullname" . }}-backend -o jsonpath="{.spec.ports[0].port}")
  export BACKEND_HOST=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "todo-app.fullname" . }}-backend -o jsonpath="{.spec.clusterIP}")
  echo "Visit http://$BACKEND_HOST:$BACKEND_PORT to access the backend"